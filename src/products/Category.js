const Category = ({ item }) => {
  return (
    <div className="txtConbox">
      <div className="listTit">
        <strong># {item.title}</strong>
      </div>
    </div>
  );
};
export default Category;
